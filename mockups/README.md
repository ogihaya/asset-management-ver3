# mockups

要件確認用の独立モックアップアプリです。`frontend/` とは別アプリとして動作します。

## 起動

```bash
cd mockups
npm install
npm run dev
```

## デモログイン

- メールアドレス: `demo@example.com`
- パスワード: `Demo1234!`

## デモリセット

ブラウザで `?reset-demo=1` を付けてアクセスすると、保存済みデータを初期状態に戻します。
例: `http://localhost:4174/login?reset-demo=1`
